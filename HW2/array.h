#pragma once

#include "util.h"

class Array {
    public:
        void reverse(char a[]);
        int string2number(const char* s);
        int removedups(int a[], int b);
        void p(string s, int b[], int l);
        void p(string s, char a[]);
        void copy(char a[],const char* b);
    private:

};
